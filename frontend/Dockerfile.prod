# 운영 환경용 프론트엔드 Dockerfile
FROM node:18-alpine as builder

# 작업 디렉토리 설정
WORKDIR /app

# package.json과 package-lock.json 복사
COPY package*.json ./

# 의존성 설치 (빌드에 필요한 모든 의존성 포함)
RUN npm ci

# 소스 코드 복사
COPY . .

# 애플리케이션 빌드 (TypeScript 체크 건너뛰기)
RUN npx vite build

# 실행 단계
FROM nginx:alpine

# 빌드된 파일을 nginx에 복사
COPY --from=builder /app/dist /usr/share/nginx/html

# nginx 설정 파일 복사
COPY nginx.conf /etc/nginx/nginx.conf

# 포트 노출
EXPOSE 80

# nginx 실행
CMD ["nginx", "-g", "daemon off;"]
